# 02. 개발 계획 · 로드맵

## 원칙

1. **항상 플레이 가능한 상태 유지.** 매 마일스톤 끝에 `main` 이 Pages 에 배포되고 처음부터 끝까지(현재 구현 범위) 플레이된다.
2. **스토리 먼저, 그림 나중.** 전 챕터를 플레이스홀더 그래픽으로 완주한 뒤 에셋을 교체한다. 소설이 이미 완성돼 있으므로 텍스트 파이프라인이 가장 빠른 길이다.
3. **코드보다 데이터.** 챕터는 JSON. 새 챕터 = 새 JSON 파일 + 레지스트리 등록. 엔진 코드를 건드리는 건 새 연출 명령이 필요할 때뿐.
4. **커밋 단위 = 챕터 or 시스템 하나.** `validate:script` 와 `typecheck` 가 통과해야 push.

## 마일스톤

### M0 — 프로젝트 셋업 ✅ (2026-09-12)

- [x] Vite + TypeScript + Phaser 3 스캐폴드
- [x] 사이드스크롤 이동 · 트리거 · 대화창 · 선택지 · 플래그 · 세이브
- [x] 챕터 1~3 스크립트 (부 I 전체)
- [x] 절차적 플레이스홀더 (배경 그라데이션 · 캐릭터 실루엣)
- [x] 스크립트 검증 스크립트, GitHub Pages 배포 워크플로
- [x] 기획·기술 문서

### M1 — 코어 완성 ✅ (2026-09-12)

목표: 부 II~V 를 쓰는 데 필요한 연출 명령을 전부 갖춘다.

- [x] BGM 재생·크로스페이드 (`AudioManager`), `se` 명령
- [x] 이탤릭 `*...*` 레드 톤 렌더 (`RichText` — 단어 단위 배치, 타자 효과 호환)
- [x] 화면 효과 명령: `fx shake / flash / tint(cold|warm|chaos|none)`
- [x] `face` 파라미터·명령으로 표정 스프라이트 교체
- [x] VN 모드(world 없음) — 배우 발끝을 대화창 윗선에 맞춤
- [x] 백로그(L·휠 위) · Ctrl 스킵(읽은 줄만, 세이브와 별개로 영구 기록)
- [x] 타이틀 이름 입력 DOM UI (한글 IME), `?chapter=` 바로 진입
- [x] 설정(텍스트 속도·BGM·SE 볼륨) — Esc 메뉴 / 타이틀
- [x] 조연 화자 14명 등록 + 실루엣 플레이스홀더, 데모 챕터 `demo`
- [ ] ScriptRunner 단위 테스트 (Vitest) — M2 진행하며 필요해지면

완료 기준 충족: `?chapter=demo` 가 모든 명령을 실행하고, 1~3장 회귀 없음.

### M2 — 부 II 「기록」 (4~8장) ✅ (2026-09-12)

- [x] ch04~ch08 JSON (소설 §4 → 스크립트. 원문 유지, `{name}` 치환만)
- [x] 3장 끝에 회상 진입 연출 ("머무는 며칠, 노아는 조각을 모았다") → ch04
- [x] 8장 5단 시퀀스: 각성(shake+레드 flash, 냉정 표정) → 소원(시안 flash, 쌍둥이 부활) → 멸망(레드 flash 두 번) → 폭주(`tint chaos` 4초 → 암전 → 폐허) → 통곡(눈물 표정, 웜 톤)
- [x] 화자 추가: `bel`(회상 속 어린 베르), `court`, `mother` + `baelz_child_cold` / `baelz_child_cry` 실루엣
- [x] 6장에서 `pebble_story` 플래그 (히든 엔딩 조건용)
- [x] `scripts/format-chapters.mjs` — 챕터 JSON 한 줄 스타일 정리

완료 기준 충족: ch01 → ch08 연속 플레이, 런타임 에러 없음.

**기획 메모 (사용자 확인 필요)**: 소설 원문은 회상(4~8장)에서 아이를 "베르"라고 부른다. 3장에서 리사가 "베르—!" 하고 부르므로 플레이어가 4장에서 '숲의 아이 = 예언의 아이'를 눈치챌 수 있다. 원본 §5-2 의 "11장에서 정체 공개" 의도를 지키려면 회상 속 호칭을 "공주"/"아이"로 가리는 선택지가 있다 — 원문 수정이라 결정은 사용자 몫. 현재는 원문 그대로.

### M3 — 부 III 「머무는 날들」 (9~12장) ✅ (2026-09-12)

- [x] ch09~ch12 JSON. 9·10장 탐색형(트리거 4~5개, E 로 말 걸기), 11·12장 VN
- [x] 9장 아카시아 개그 — 선택지 2개(정정 / 웃음 참기) → 미사 팩폭 → 발끈 3콤보로 합류
- [x] 11장 리사의 "왕님" 실수 → BGM 정지 + 콜드 톤으로 정체 공개. 뿔 소년 "아무도 불러 준 적이 없어서요" 뒤 3.5초 침묵
- [x] 12장 자기 소개 선택지 `ch12_intro` (vessel / nothing / silent)
- [x] 히든 조건 플래그: `ch09_stories`, `ch10_elder`, `ch10_woman` (부 III 선택 트리거 전부) + `pebble_story`(6장)
- [x] 엔진: 타자 중 Space/클릭 = 즉시 전체 표시(그동안 안 먹던 버그), 선택지 중 이름표 숨김

완료 기준 충족: ch01 → ch12 연속 플레이, 런타임 에러 없음.

**텍스트 메모**: 선택지 분기 대사(2장 "예쁜 눈이라서", 12장 "거짓말." / "…됐어. 안 물어볼게.")는 원문에 없는 최소 추가. 원본 §6-1 이 정한 4개 선택지에만 한정.

### M4 — 부 IV·V + 엔딩 (13~16장, 에필로그) ✅ (2026-09-12) → **v0.9**

- [x] ch13 두 얼굴: 탐색형. 노예상 대치에서 BGM 정지 → 콜드 톤 + `baelz_cold` 표정 + CG → 시안 플래시(채찍 소멸) → 웜 톤 복귀
- [x] ch14 지워진 것들: **엔딩 분기** `ending` = `pity` / `respect` / `awe`. 선택 후 각자 한두 줄 → "둘은 그저, 잿빛이 된 자리를 함께 오래 바라보았다" 5초 침묵으로 합류
- [x] ch15 혼자였던 적: 암전 → `risa_bare` / `misa_bare` 맨얼굴 등장 + CG
- [x] ch16 기록자의 선물: 페이지 넘김 SE, CG, `baelz_cry` 표정
- [x] 에필로그: 성문 배웅 → 세월 → `if ending` 3분기 마무리 문장 → 공통 결말 → 히든 조약돌 씬(`pebble_story` ∧ `ch09_stories` ∧ `ch10_elder` ∧ `ch10_woman`) → "— 끝."
- [x] 엔딩 후 타이틀: **다시 읽기(챕터 선택)**, 달성 엔딩 표시(연민·존중·경외·조약돌)
- [x] 엔진: 색조 오버레이를 CG 위로(depth 650), HUD 챕터 번호 `EP`

완료 기준 충족: **ch01 → 에필로그 전편 완주 가능 (플레이스홀더 그래픽)**. 엔딩 존중 루트·히든 씬 브라우저 확인.

**히든 씬 텍스트**: "그리고 그 노트 위에 — 서툴게 색칠한 조약돌 하나. 누가 준 것인지, 이 나라에서 아는 사람은 셋뿐이다." — 원본 §6-3 아이디어를 바탕으로 새로 쓴 두 줄. 원문에는 없음.

### M5 — 에셋 · 폴리시 · 릴리즈 (진행 중)

**코드 쪽 (완료, 2026-09-12)**
- [x] 에셋 파이프라인: `scripts/asset_prompts.json`(§7 프롬프트) → `gen_assets.py`(diffusers) → `prep_assets.py`(리사이즈·rembg·manifest). 합성 이미지로 end-to-end 검증
- [x] 실제 배경이 있으면 플레이스홀더 나무 실루엣 생략, `ui/title.png` 있으면 타이틀 아트로 교체
- [x] 터치 조작: 가상 ◀ ▶ ● 버튼(터치 기기만), 탭 진행, 세로 화면 회전 안내
- [x] 오디오 자동재생 정책 대응 (첫 입력 후 BGM 시작)
- [x] 파비콘(뱀눈 주사위 SVG), OG 메타, 타이틀 플레이스홀더 모티프
- [x] [05_에셋_가이드.md](05_에셋_가이드.md)

**에셋 (2026-09-12, Illustrious-XL v0.1 · RTX 4070 Ti)**
- [x] 배경 12장 — 1차 결과에서 인물 혼입·구도 불일치 6장 재생성. `no people` 보다 danbooru 태그 `no humans, scenery` 가 잘 먹음
- [x] 스프라이트 23종 — `hakos baelz` 태그는 상반신으로 쏠려서 `full body, standing, straight-on` + negative `portrait, upper body, cowboy shot` 로 조정. rembg 는 `isnet-anime` 기본, 키별 `u2net` 오버라이드
- [x] CG 14컷 + 타이틀 아트 — 각 2~3장 뽑아 선택. 타이틀은 그림이 있을 때 글자를 왼쪽 기둥으로 배치
- [ ] **2차 품질 패스** — 스타일 통일(일부 CG 는 유화풍/일부는 애니 클린), 벨즈 스프라이트 두상 비율, 4장 CG 에 왕·아기 부재, 11장 CG 구도. 필요하면 LoRA
- [ ] BGM 17곡 + 메인 테마, 효과음 5종 (`page`, `tear`, `dissolve`, `gate`, `wind`) — 외부 서비스(mureka 등)
- [ ] 톤 통일(레드·시안 그레이딩)

후보·탈락본은 `raw-assets/_rejected/` (git 무시).

**릴리즈**
- [ ] 실기기 테스트 (Chrome/Edge/Firefox/모바일 Safari)
- [ ] itch.io 페이지 + GitHub Release v1.0 (v0.9 는 draft 로 올려 둠)

## 순서를 이렇게 잡은 이유

- M1 을 M2 앞에 두는 이유: 회상 챕터(4~8장)는 화면 효과·표정 교체·BGM 없이는 쓰기 어렵다. 명령이 없으면 JSON 을 나중에 다시 고쳐야 한다.
- 에셋을 마지막에 두는 이유: AI 생성 에셋은 "어떤 컷이 실제로 필요한지" 플레이해 보고 결정하는 게 낭비가 적다. 특히 CG 는 플레이스홀더 상태에서 씬 타이밍을 먼저 잡는다.

## 작업 방식 (Claude Code 와 함께)

- 새 챕터: "ch04 를 소설 §4 「예언」에서 변환해 줘" → JSON 작성 → `npm run validate:script` → 브라우저에서 확인 → 커밋.
- 새 명령: `types.ts` 에 타입 추가 → `ScriptRunner.exec` 에 케이스 → `StageHooks` 에 훅 → `ChapterScene` 구현 → `validate-chapters.mjs` 허용 목록 추가.
- 에셋 교체: 파일을 `public/assets/<종류>/` 에 넣고 `manifest.json` 배열에 키 추가. 코드 수정 없음.

## 브랜치 · 배포

- `main` = 항상 배포 가능. push 시 GitHub Pages 자동 배포.
- 챕터·기능은 `feat/ch04`, `feat/bgm` 같은 브랜치에서 작업 후 머지 (혼자 개발이면 `main` 직접 커밋도 허용, 단 validate/typecheck 통과 필수).
- 태그: `v0.1` (M0), `v0.5` (M3), `v0.9` (M4), `v1.0` (M5).
